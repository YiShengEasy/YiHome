import React from 'react';
import { connect } from 'dva';

function IndexPage() {
  return (
    <div>
      aaaa   
    </div>
  );
}

IndexPage.propTypes = {
};

export default connect()(IndexPage);
